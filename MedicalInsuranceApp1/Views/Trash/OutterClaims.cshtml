@model List<MedicalInsuranceApp1.Models.Entities.OutterClaim>
@{ ViewData["Title"] = "محذوفات مطالبات القضايا"; }
<div style="margin-bottom:20px">
    <h2 style="font-size:18px;font-weight:700;color:#ffffff">🗑 محذوفات مطالبات القضايا</h2>
    <p style="font-size:13px;color:#9a9890;margin-top:2px">إجمالي @Model.Count عنصر محذوف</p>
</div>
<div class="table-card">
    <div class="table-hdr"><div class="chart-title">المطالبات المحذوفة</div></div>
    <table class="data-table">
        <thead><tr><th>رقم المطالبة</th><th>المدعي</th><th>الفرع</th><th>تاريخ الحذف</th><th>حذف بواسطة</th><th>السبب</th><th>إجراء</th></tr></thead>
        <tbody>
            @if (!Model.Any()) {
                <tr><td colspan="7" style="text-align:center;padding:40px;color:#9a9890">لا توجد مطالبات محذوفة</td></tr>
            } else {
                @foreach (var x in Model) {
                    <tr>
                        <td><strong style="color:#1B4332">@x.Year-@x.Num</strong></td>
                        <td>@(x.Plaintiff?.PlainitiffName ?? "-")</td>
                        <td>@(x.Branch?.BranchName ?? "-")</td>
                        <td style="font-size:12px;color:#9a9890">@x.DeletedAt?.ToString("yyyy/MM/dd HH:mm")</td>
                        <td>@(x.DeletedBy ?? "-")</td>
                        <td><span class="badge badge-courts">@(x.DeleteReason ?? "غير محدد")</span></td>
                        <td>
                            <form action="/Trash/RestoreOutterClaim" method="post" style="display:inline">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="id" value="@x.Id" />
                                <button type="submit" class="btn-sm" style="color:#2d6a4f;border-color:#9AB17A"><i class="ti ti-restore"></i> استرجاع</button>
                            </form>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>
@section Styles { <link rel="stylesheet" href="~/css/dashboard.css"> }
