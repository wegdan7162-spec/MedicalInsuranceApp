@model List<MedicalInsuranceApp1.Models.Entities.Branch>
@{ ViewData["Title"] = "محذوفات الفروع"; }
<div style="margin-bottom:20px">
    <h2 style="font-size:18px;font-weight:700;color:#ffffff">🗑 محذوفات الفروع</h2>
    <p style="font-size:13px;color:#9a9890;margin-top:2px">إجمالي @Model.Count عنصر محذوف</p>
</div>
<div class="table-card">
    <div class="table-hdr"><div class="chart-title">الفروع المحذوفة</div></div>
    <table class="data-table">
        <thead><tr><th>اسم الفرع</th><th>تاريخ الحذف</th><th>حذف بواسطة</th><th>السبب</th><th>إجراء</th></tr></thead>
        <tbody>
            @if (!Model.Any()) {
                <tr><td colspan="5" style="text-align:center;padding:40px;color:#9a9890">لا توجد فروع محذوفة</td></tr>
            } else {
                @foreach (var x in Model) {
                    <tr>
                        <td><strong>@x.BranchName</strong></td>
                        <td style="font-size:12px;color:#9a9890">@x.DeletedAt?.ToString("yyyy/MM/dd HH:mm")</td>
                        <td>@(x.DeletedBy ?? "-")</td>
                        <td><span class="badge badge-courts">@(x.DeleteReason ?? "غير محدد")</span></td>
                        <td>
                            <form action="/Trash/RestoreBranch" method="post" style="display:inline">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="id" value="@x.Id" />
                                <button type="submit" class="btn-sm" style="color:#2d6a4f;border-color:#9AB17A" title="استرجاع"><i class="ti ti-restore"></i> استرجاع</button>
                            </form>
                            <form action="/Trash/DeleteBranch" method="post" style="display:inline"
                                  onsubmit="return confirm('⚠ هذا حذف نهائي لا يمكن التراجع عنه. هل أنت متأكد؟')">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="id" value="@x.Id" />
                                <button type="submit" class="btn-sm" style="color:#c0392b;border-color:#e74c3c"><i class="ti ti-trash-x"></i> حذف نهائي</button>
                            </form>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>
@section Styles { <link rel="stylesheet" href="~/css/dashboard.css"> }
