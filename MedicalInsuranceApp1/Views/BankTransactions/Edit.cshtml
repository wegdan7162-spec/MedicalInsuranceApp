@model MedicalInsuranceApp1.Models.Entities.BankTransaction
@using MedicalInsuranceApp1.Models.Entities
@{
    ViewData["Title"] = "تعديل حركة مصرفية";
}

<div style="display:flex;align-items:center;gap:12px;margin-bottom:24px">
    <a href="/BankTransactions" class="btn-back"><i class="ti ti-arrow-right"></i></a>
    <div>
        <h2 style="font-size:18px;font-weight:700;color:#ffffff">تعديل حركة مصرفية</h2>
        <p style="font-size:13px;color:#9a9890;margin-top:2px">قيد @(Model.EntryNumber ?? Model.Id.ToString())</p>
    </div>
</div>

<div class="table-card" style="max-width:760px">
    <form asp-action="Edit" asp-route-id="@Model.Id" method="post">
        @Html.AntiForgeryToken()

        <div class="form-grid">
            <div class="form-group" style="grid-column:1/-1">
                <label class="form-label">الحساب المصرفي</label>
                <select asp-for="BankAccountId" class="form-input" disabled>
                    @foreach (var a in ViewBag.Accounts)
                    {
                        <option value="@a.Id">@a.BankName — @a.AccountNumber</option>
                    }
                </select>
                <span style="font-size:11px;color:#9a9890">لتغيير الحساب احذفي الحركة وأضيفيها على الحساب الصحيح</span>
                <input type="hidden" name="BankAccountId" value="@Model.BankAccountId" />
            </div>

            <div class="form-group">
                <label class="form-label">تاريخ الحركة</label>
                <input asp-for="TransactionDate" type="date" class="form-input" />
            </div>
            <div class="form-group">
                <label class="form-label">رقم القيد</label>
                <input asp-for="EntryNumber" class="form-input" />
            </div>
            <div class="form-group">
                <label class="form-label">نوع المستند</label>
                <select asp-for="DocType" class="form-input">
                    <option value="">— اختر —</option>
                    @foreach (DocumentType dt in ViewBag.DocTypes)
                    {
                        <option value="@((int)dt)">@dt.ToString().Replace("_"," ")</option>
                    }
                </select>
            </div>
            <div class="form-group">
                <label class="form-label">رقم الصك</label>
                <input asp-for="DocumentNumber" class="form-input" />
            </div>

            <div class="form-group" style="background:#fde8e8;padding:14px;border-radius:10px;border:1px solid #f5c6c6">
                <label class="form-label" style="color:#c0392b">مبلغ مدين د.ل</label>
                <input asp-for="DebitAmount" type="number" step="0.001" min="0" class="form-input"
                       style="font-size:16px;font-weight:700;color:#c0392b" />
            </div>
            <div class="form-group" style="background:#e8f5ee;padding:14px;border-radius:10px;border:1px solid #b7dfc8">
                <label class="form-label" style="color:#2d6a4f">مبلغ دائن د.ل</label>
                <input asp-for="CreditAmount" type="number" step="0.001" min="0" class="form-input"
                       style="font-size:16px;font-weight:700;color:#2d6a4f" />
            </div>

            <div class="form-group">
                <label class="form-label">تصنيف الحركة</label>
                <select asp-for="Category" class="form-input">
                    @foreach (TransactionCategory cat in ViewBag.Categories)
                    {
                        <option value="@((int)cat)">@cat.ToString().Replace("_"," ")</option>
                    }
                </select>
            </div>
            <div class="form-group">
                <label class="form-label" style="color:#9a9890">ربط بملف ودي</label>
                <select asp-for="FriendlyClaimId" class="form-input">
                    <option value="">— لا يوجد —</option>
                    @foreach (var c in ViewBag.FriendlyClaims)
                    {
                        <option value="@c.Id">@c.Label</option>
                    }
                </select>
            </div>
            <div class="form-group">
                <label class="form-label" style="color:#9a9890">ربط بملف قضائي</label>
                <select asp-for="OutterClaimId" class="form-input">
                    <option value="">— لا يوجد —</option>
                    @foreach (var c in ViewBag.OutterClaims)
                    {
                        <option value="@c.Id">@c.Label</option>
                    }
                </select>
            </div>

            <div class="form-group" style="grid-column:1/-1">
                <label class="form-label">البيان</label>
                <textarea asp-for="Description" class="form-input" rows="3"></textarea>
            </div>
        </div>

        <div style="background:#fff8e8;border:1px solid #f0d080;border-radius:8px;padding:12px;margin-top:16px;font-size:12px;color:#7a6020">
            <i class="ti ti-info-circle" style="margin-left:6px"></i>
            بعد الحفظ يُنصح بالذهاب للحساب المصرفي واستخدام «إعادة حساب» لتحديث الأرصدة الجارية بشكل صحيح
        </div>

        <div style="display:flex;gap:10px;margin-top:16px">
            <button type="submit" class="btn-primary"><i class="ti ti-device-floppy"></i> حفظ</button>
            <a href="/BankTransactions" class="btn-cancel">إلغاء</a>
        </div>
    </form>
</div>

@section Styles {
    <link rel="stylesheet" href="~/css/claims.css">
    <style>
    .form-grid{display:grid;grid-template-columns:1fr 1fr;gap:16px;}
    .form-group{display:flex;flex-direction:column;gap:6px;}
    .form-label{font-size:13px;font-weight:600;}
    .form-input{padding:10px 12px;border:1px solid #e0e0d8;border-radius:8px;font-family:Cairo,sans-serif;font-size:13px;background:var(--input-bg,#fafaf8);}
    .form-input:focus{outline:none;border-color:#1B4332;box-shadow:0 0 0 3px rgba(27,67,50,0.1);}
    </style>
}
